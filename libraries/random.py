import random
# i've decided to create a game with these following themes -- inspired by the Philippine Holidays
# but can't decide which one to use... so i let random py to choose for me
# anyway -- valor will be the theme of my game
themes = random.choice(["valor", "easter", "labor", "christmas", "all saints"])
print(themes)